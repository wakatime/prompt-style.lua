# https://docs.readthedocs.io/en/stable/config-file/v2.html
---
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3"
  apt_packages:
    - lua-ldoc
    - lua-discount
  commands:
    - ldoc .
    - mv doc $READTHEDOCS_OUTPUT/html
